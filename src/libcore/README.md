# libcore

Core utility library providing logging (`log_err`, `log_warn`, `log_info`,
`log_debug`), safe memory allocation wrappers that abort on failure
(`xmalloc`, `xcalloc`, `xrealloc`, `xstrdup`), portable `strlcpy`/`strlcat`
string helpers, and PATH-like directory searching for sub-command executables.
