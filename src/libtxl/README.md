# libtxl

Terminal translation engine. Loads data-driven translation rules from
configuration and maps abstract terminal operations to concrete escape
sequences, keeping control code handling out of hard-coded logic.
