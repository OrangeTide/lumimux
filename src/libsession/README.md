# libsession

Session and window lifecycle management. A session is a named container
created with `session_new`; windows live inside a session and are spawned
with `window_new`, which starts a shell in a new PTY.
